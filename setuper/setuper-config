{
    "settings": {
        "global-setup": {},
        "system-setup": {
            "ubuntu": { "package-manager": "sudo apt install {}" },
            "archlinux": { "package-manager": "sudo pacman -S {}" }
        }
    }
}

